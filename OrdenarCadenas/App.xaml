<Window x:Class="OrdenarCadenas.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ordenar Cadenas" Height="450" Width="600">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <Label Content="Ingrese nombres separados por comas o espacios:" FontWeight="Bold"/>
            <TextBox x:Name="txtCadena" Height="30" TextWrapping="Wrap" VerticalContentAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="btnListar" Content=" &lt;&lt; Listar &gt;&gt; " Width="100" Height="25" FontWeight="Bold"/>
        </StackPanel>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListBox x:Name="lstIzquierda" Grid.Column="0"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="10,0">
                <Label Content="Letra:" HorizontalAlignment="Center"/>
                <TextBox x:Name="txtLetra" Width="40" Height="25" TextAlignment="Center" Margin="0,0,0,10"/>
                <Button x:Name="btnPasar" Content=" &lt;&lt; Pasar &gt;&gt; " Width="80" Height="25" FontWeight="Bold"/>
            </StackPanel>

            <ListBox x:Name="lstDerecha" Grid.Column="2"/>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Label Content="Total elementos:" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox x:Name="txtTotal1" Width="50" Height="25" IsReadOnly="True" TextAlignment="Center" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

            <Label Content="Total filtrados:" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox x:Name="txtTotal2" Width="50" Height="25" IsReadOnly="True" TextAlignment="Center" VerticalContentAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>